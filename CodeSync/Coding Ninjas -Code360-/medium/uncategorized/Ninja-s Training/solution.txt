/*
 * Platform: Coding Ninjas (Code360)
 * Problem: Ninja’s Training
 * URL: https://www.naukri.com/code360/problems/longest-palindromic-substring_758900
 * Language: Java (SE 1.8)
 * Difficulty: Medium
 * Topics: Uncategorized
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-06-03T12:41:07.367Z
 */

for (int i = 0; i < str.length(); i++) {
            // Case 1: Odd length palindrome (center at i)
            int len1 = expandAroundCenter(str, i, i);
            // Case 2: Even length palindrome (center between i and i+1)
            int len2 = expandAroundCenter(str, i, i + 1);
            
            int currentMaxLength = Math.max(len1, len2);
            
            if (currentMaxLength > maxLength) {
                maxLength = currentMaxLength;
                // Calculate the starting index based on the palindrome length
                start = i - (currentMaxLength - 1) / 2;
            }
        }
        
        int maxLength = 1;
        int start = 0;
        
        }
            return str;
        if (str.length() == 1) {
        
        }
            return "";
        if (str == null || str.length() == 0) {
    public static String longestPalinSubstring(String str) {
        
        return str.substring(start, start + maxLength);
    }
