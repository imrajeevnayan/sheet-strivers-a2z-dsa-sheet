/*
 * Platform: LeetCode
 * Problem: test case :-
 * URL: https://leetcode.com/problems/house-robber-ii/?envType=problem-list-v2&envId=dxl0w3a1
 * Language: Blind 75 (Copy-oizxjoit)
 * Difficulty: Medium
 * Topics: Array, Dynamic Programming
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-06-08T10:13:32.961Z
 */

int option1 = robLinear(nums, 0, nums.length - 2);
        
        // Scenario 2: Exclude the first house
        int option2 = robLinear(nums, 1, nums.length - 1);
        
        return Math.max(option1, option2);
    }
    
    // Helper method to solve standard House Robber I for a sub-array
    private int robLinear(int[] nums, int start, int end) {
        int prev1 = 0; // Max profit up to i-1
        int prev2 = 0; // Max profit up to i-2
        
        for (int i = start; i <= end; i++) {
            int current = Math.max(prev1, prev2 + nums[i]);
        // Scenario 1: Exclude the last house
        
        if (nums == null || nums.length == 0) return 0;
        if (nums.length == 1) return nums[0];
class Solution {
    public int rob(int[] nums) {
