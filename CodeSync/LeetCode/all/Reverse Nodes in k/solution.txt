/*
 * Platform: LeetCode
 * Problem: Reverse Nodes in k
 * URL: https://leetcode.com/problems/reverse-nodes-in-k-group/?spm=a2ty_o01.29997173.0.0.160c55fbqASZ87
 * Language: Problem List
 * Difficulty: Hard
 * Topics: Linked List, Recursion
 * Runtime: 0 ms
 * Memory: N/A
 * Synced: 2026-06-11T04:35:31.961Z
 */

curr = next;
                prev = curr;
                curr.next = prev;
                ListNode next = curr.next;
            while (curr != nextGroupStart) {
            ListNode nextGroupStart = kthNode.next;
            ListNode prev = nextGroupStart, curr = head;
            }

            if (temp == null) newHead = prev;
            else temp.next = prev;
    
            temp = head;
            head = nextGroupStart;
        }

        return newHead != null ? newHead : head;
    }
}
