/*
 * Platform: LeetCode
 * Problem: Remove Nodes From Linked List
 * URL: https://leetcode.com/problems/remove-nodes-from-linked-list/description/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Linked List, Stack, Recursion, Monotonic Stack
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-06-09T18:24:00.477Z
 */

class Solution {
    public ListNode removeNodes(ListNode head) {
        Deque<ListNode> stack = new ArrayDeque<>();
        
        // Traverse and maintain monotonic decreasing stack
        ListNode curr = head;
        while (curr != null) {
            // Remove all nodes smaller than current (they have greater node to right)
            while (!stack.isEmpty() && stack.peek().val < curr.val) {
                stack.pop();
            }
            stack.push(curr);
            curr = curr.next;
        }
        
        // Rebuild result list from stack (top of stack = last surviving node)
        ListNode next = null;
        while (!stack.isEmpty()) {
