/*
 * Platform: LeetCode
 * Problem: Code
 * URL: https://leetcode.com/problems/remove-nodes-from-linked-list/submissions/2027364518/
 * Language: Problem List
 * Difficulty: Medium
 * Topics: Linked List, Stack, Recursion, Monotonic Stack
 * Runtime: 0 ms
 * Memory: 130.56 MB
 * Synced: 2026-06-09T09:57:31.329Z
 */

head = head.next;
        }
        curr.next = null; // Terminate the filtered list
        
            }
                maxVal = head.val;
                curr.next = head;
                curr = curr.next;
        while (head != null) {
            if (head.val >= maxVal) {
        int maxVal = Integer.MIN_VALUE;
        
        ListNode dummy = new ListNode(0);
        ListNode curr = dummy;
        // Step 2: Traverse and keep nodes >= current max
        
        head = reverse(head);
        // Step 1: Reverse the list
    public ListNode removeNodes(ListNode head) {
class Solution {
