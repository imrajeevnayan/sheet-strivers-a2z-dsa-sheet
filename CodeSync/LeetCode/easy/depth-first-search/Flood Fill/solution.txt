/*
 * Platform: LeetCode
 * Problem: Flood Fill
 * URL: https://leetcode.com/problems/flood-fill/?envType=problem-list-v2&envId=dxlq3az1
 * Language: Grind 75 (Copy-rab78cw1)
 * Difficulty: Easy
 * Topics: Array, Depth-First Search, Breadth-First Search, Matrix
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-06-03T20:06:57.762Z
 */

return image;
    }
    
    private void dfs(int[][] image, int row, int col, int originalColor, int newColor) {
        // Boundary check and color match check
        if (row < 0 || row >= image.length || 
            col < 0 || col >= image[0].length || 
            image[row][col] != originalColor) {
            return;
        }
        
        // Change color
        image[row][col] = newColor;
        dfs(image, sr, sc, originalColor, color);
        if (originalColor == color) return image;
        int originalColor = image[sr][sc];
class Solution {
    public int[][] floodFill(int[][] image, int sr, int sc, int color) {
        
        // Recurse in 4 directions
        dfs(image, row + 1, col, originalColor, newColor); // Down
