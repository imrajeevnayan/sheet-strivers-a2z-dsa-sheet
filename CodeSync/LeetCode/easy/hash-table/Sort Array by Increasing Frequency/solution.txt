/*
 * Platform: LeetCode
 * Problem: Sort Array by Increasing Frequency
 * URL: https://leetcode.com/problems/sort-array-by-increasing-frequency/
 * Language: Problem List
 * Difficulty: Easy
 * Topics: Array, Hash Table, Sorting
 * Runtime: 2 ms
 * Memory: N/A
 * Synced: 2026-06-07T09:56:50.921Z
 */

if (!map.get(a).equals(map.get(b))) {
                return map.get(a) - map.get(b); // Lower freq first
            }
            return b - a; // Higher value first (for same freq)
        });

        // Add all numbers to the heap
        for (int num : nums) {
            pq.offer(num);
        }

        PriorityQueue<Integer> pq = new PriorityQueue<>((a, b) -> {
        
        for (int num : nums)map.put(num, map.getOrDefault(num, 0) + 1);
        Map<Integer, Integer> map = new HashMap<>();
class Solution {
    public int[] frequencySort(int[] nums) {
        // 3. Poll from heap to build result
        int[] result = new int[nums.length];
        for (int i = 0; i < nums.length; i++) {
            result[i] = pq.poll();
