/*
 * Platform: GeeksforGeeks
 * Problem: Pair Sum in BST
 * URL: https://www.geeksforgeeks.org/batch/gfg-160-problems/track/tree-gfg-160/problem/inorder-traversal
 * Language: Java (21)
 * Difficulty: Unknown
 * Topics: Expected Complexities, Company Tags, VMWare, Flipkart, Amazon, Microsoft, OYO Rooms, Snapdeal
 * Runtime: N/A
 * Memory: N/A
 * Synced: 2026-06-13T16:21:55.725Z
 */

class Solution {
    boolean findTarget(Node root, int target) {
        HashSet<Integer> set = new HashSet<>();
        return findTargetHelper(root, target, set);
    }
    
    private boolean findTargetHelper(Node root, int target, HashSet<Integer> set) {
        if (root == null) {
            return false;
        
        
        // Check if complement exists
        if (set.contains(target - root.data)) {
            return true;
        }
        
        // Add current node's data to set
        set.add(root.data);
        
        // Check left and right subtrees
        return findTargetHelper(root.left, target, set) || 
               findTargetHelper(root.right, target, set);
    }
}
